import { removeUnicode } from '../../../helpers/remove-unicode';
import { module, test } from 'qunit';

module('Unit | Helper | remove unicode');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = removeUnicode(42);
  assert.ok(result);
});
