<div class="row">
  <div class="col-md-4">
    <div class="list-group">
      {{#each model.masterPkgList as |pkg|}}
        {{#link-to "package-list.pkg-info" pkg class="list-group-item"}}
          {{pkg}}
        {{/link-to}}
      {{/each}}
    </div>
  </div>
  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
