<div class="row">
  <div class="col-md-4">
    {{package-list
      packages=model
      onSearch=(action "showPkgInfo")
      delete=(action "removePackage")
      undo=(action "undoRemove")
      confirm=(action "confirmRemove")
    }}
  </div>
  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
