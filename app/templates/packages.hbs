<div class="row">
  <div class="col-md-4">
    <div class="list-group">
      {{#each model as |pkg|}}
        {{#link-to "packages.pkg-info" pkg.id class="list-group-item"}}
          {{pkg.name}}
          {{pkg.id}}
        {{/link-to}}
      {{/each}}
    </div>
  </div>
  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
